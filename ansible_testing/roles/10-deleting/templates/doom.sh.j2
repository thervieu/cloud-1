#!/bin/bash

echo -e "#### DESTROYING CONTAINERS, CUSTOM IMAGES AND NETWORK\n"

sudo docker kill $(docker ps -q)
sudo docker rmi -f "{{ db_image }}" "{{ pma_image }}" "{{ wp_image }}" "{{ httpd_image }}"
sudo docker network rm "{{network_name}}"
