<VirtualHost *:80>

    ServerName anyastu.fr

#    LoadModule ssl_module modules/mod_ssl.so

#    SSLProxyEngine on

#    RequestHeader set X-Forwarded-Proto “https”
#    RequestHeader set X-Forwarded-Port “443”

#    ProxyPreserveHost On
        
    # Servers to proxy the connection, or;
    # List of application servers:
    # Usage:
    # ProxyPass / http://[IP Addr.]:[port]/
    # ProxyPassReverse / http://[IP Addr.]:[port]/
    # Example: 

    ProxyPass /mwi https://www.middlewareinventory.com
    ProxyPassReverse /mwi https://www.middlewareinventory.com

    ProxyPass /phpmyadmin http://172.20.0.3:80/
    ProxyPassReverse /phpmyadmin http://172.20.0.3:80/

    ProxyPass / http://172.20.0.4:80/
    ProxyPassReverse / http://172.20.0.4:80/
        
</VirtualHost>
